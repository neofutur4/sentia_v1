name: sentia
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # ACTUALIZADO: Subimos a la versión 2.12.0 para que soporte AppLinks nuevo
  supabase_flutter: ^2.12.0
  cupertino_icons: ^1.0.2
  # Mantenemos este para que Android V2 funcione
  app_links: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
