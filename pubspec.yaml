name: sentia
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Mantenemos tu versión de Supabase
  supabase_flutter: ^1.10.25
  cupertino_icons: ^1.0.2
  # ESTA es la línea mágica que arregla el error de compilación
  app_links: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
